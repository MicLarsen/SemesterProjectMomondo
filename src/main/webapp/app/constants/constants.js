"use strict";

/**
 * This is module where we define our constants used throughout the application.
 */

angular.module('myApp').constant('cfg', {
    baseurl: 'http://localhost:8080/' // @todo: This is used in the data factory. Change this to your API's base URL (WITH trailing slash)
});